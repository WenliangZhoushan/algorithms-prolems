#include <bits/stdc++.h>
using namespace std;

const int MAXN = 200005;
int t, n, k;
int nums[MAXN];

void solve() {
  cin >> n >> k;
  for (int i = 1; i <= n; ++i) {
    cin >> nums[i];
  }
  
}

int main() {
  ios::sync_with_stdio(false);
  cin.tie(0); cout.tie(0);
  cin >> t;
  while (t--) {
    solve();
  }
}